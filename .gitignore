# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/src/generated
/reports
/downloads
/logs
/issues
/issues*
/missions
/missions*

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# hardhat
/artifacts
/cache
/typechain-types

# Info: (20260825 - Julian) foundry (forge build) 的編譯輸出：ABI 與 bytecode JSON。
# 與上面三個 hardhat 產物同一類 —— 每次 `forge build` 都會重寫，沒有任何
# src 或 scripts 引用它（實測 grep 零命中），而它一旦進版控就會在每個 PR 裡
# 冒出幾萬行 diff 稀釋 review 表面。
/forge_out
# Info: (20260826 - Julian) forge 的**編譯快取**，與 /forge_out 同一次產生（review 版控衛生）。
# 上一輪只擋了輸出、漏了快取 —— 於是 `forge_cache/solidity-files-cache.json`
# （4,476 行、內容是絕對路徑與檔案雜湊）照樣進了版控，而 develop 側零命中。
# 「同一類 build artifact」要一次擋完：漏掉的那一個不會有人發現，
# 它只是每次 build 之後安靜地出現在 `git status` 裡，然後被 `git add .` 收走。
/forge_cache

# lock files
package-lock.json
yarn.lock

# data & local seeding
/data/
/agent.md
pdfs/

# Backup Archives
*.tar.gz

# LanceDB
.lancedb_data

# Info: (20260808 - Luphia) Claude Code 本機工具設定，不入版控
.claude/

# PDF 光柵化驗證工具的相依（README 指示在該目錄 npm i；playwright 會拉數百 MB）
tools/pdf_harness/node_modules/
