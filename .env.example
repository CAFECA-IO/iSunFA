# PART 1: Core Infrastructure
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=20021
OSRM_ROUTER_URL=http://127.0.0.1:20025
CHATROOM_URL=http://127.0.0.1:20027
FAITH_PORT=20026
STORAGE_DOMAIN=http://127.0.0.1:20022
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:20024
NEXT_PUBLIC_BAIFA_EXPLORER=https://baifa.io
NEXT_PUBLIC_ISUNCOIN_CHAIN_ID=8017
REPORT_OUTPUT_DIR="reports"
MISSION_DIR="missions"
ISSUE_DIR="issues"

# PART 2: Smart Contract Deployment
NEXT_PUBLIC_DYNAMIC_KYC_MEMBERSHIP_ADDRESS=
NEXT_PUBLIC_CREDIT_POINT_ADDRESS=
NEXT_PUBLIC_MEMBERSHIP_SYSTEM_ADDRESS=
NEXT_PUBLIC_SUBSCRIPTION_MANAGER_ADDRESS=
NEXT_PUBLIC_SCW_FACTORY_ADDRESS=
NEXT_PUBLIC_ENTRY_POINT_ADDRESS=
NEXT_PUBLIC_MISSION_BOARD_ADDRESS=

# PART 3: Database Configuration
POSTGRES_DB=isunfa
POSTGRES_USER=isunfa
POSTGRES_PASSWORD=
DATABASE_URL=

# PART 4: Server SUPER ADMIN
SUPER_ADMIN_CRED_ID=
SUPER_ADMIN_PUB_X=
SUPER_ADMIN_PUB_Y=
DEWT_PRIVATE_KEY_PEM=

# PART 5: External API Configuration
NEXT_PUBLIC_APP_URL="https://isunfa.localhost"
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-ZNVVW7JP0N"
NEXT_PUBLIC_MAPTILER_KEY=

# PART 6: Configuration Immutable Signature via FIDO2
SUPER_ADMIN_SIGNATURE=

#
# PART 7: 選填參數（刻意只以註解記載，不列成鍵值）
#
# 原因：validateEnvDetailed() 會把 .env.example 的每一個鍵都當成必要鍵，
# 缺少任何一個就判定系統未初始化並重新開啟部署精靈。
# 選填參數不該讓既有部署在升級後被迫重跑初始化流程。
#
# SECRET_VAULT_MASTER_KEY
#   保護 DB 內所有密文的主密鑰（至少 32 字元高熵隨機值），以 purpose 派生子金鑰後
#   用於加密「託管錢包私鑰」與「系統設定中的秘密值」。它保護 DB 內容，
#   因此自己必須留在 env 而不能存進 DB。
#   遺失 = 託管錢包永久失效且 DB 內秘密設定無法解開；外洩 = 兩者皆遭接管。
#   由 /admin/setup 初始化資料庫時自動產生；手動補設請用：openssl rand -base64 48
#   未設定時：Google 等第三方登入無法啟用（/admin/settings 會明確標示尚未就緒）。
#
# 以下參數的正式保管位置是「資料庫的 system_setting 表」，由 /admin/setup 或
# /admin/settings 設定並經 SUPER_ADMIN passkey 簽章。在 env 設定僅作為 DB 尚未
# 設定時的 fallback（讀取優先序：DB 已驗簽 > env > 程式碼保底值）。
# 既有部署原本寫在 .env 的值會繼續生效，可在有空時再搬進 /admin/settings。
#
# GOOGLE_OAUTH_CLIENT_ID / GOOGLE_OAUTH_CLIENT_SECRET
#   FIDO2 以外的登入方式（Google）。
#   Google Cloud Console > APIs & Services > Credentials 建立 "Web application" 用戶端，
#   並將 ${NEXT_PUBLIC_APP_URL}/auth/callback/google 加入 Authorized redirect URIs。
#
# GEMINI_API_KEY / MODEL
#   LLM 金鑰與模型名稱（DB 內的鍵名為 GEMINI_API_KEY 與 LLM_MODEL）。
#   未設定時 LLM 相關功能會在呼叫時拋錯；模型名稱有程式碼預設值。
#
# OEN_ACCESS_TOKEN / OEN_MERCHANT_ID
#   應援科技金流憑證與商店代號。商店代號未設定時保底為 "mermer"。
#
# HR_PII_KEY_V1（HR_PII_KEY_V2、V3… 依此類推）
#   人事敏感個資的欄位級資料金鑰（AES-256-GCM，32 bytes base64）。
#   它保護的是 DB 欄位內容，因此與 SECRET_VAULT_MASTER_KEY 同理必須留在 env、
#   不能存進它所保護的那個資料庫（見 ADR 018 與 lib/auth/key_vault.ts 的檔頭）。
#   逐代獨立變數：輪替時**新增** V2，絕不改動既有的 V1 —— 每一列的 pii_key_version
#   決定該列要用哪一代解，改動舊值會讓那些列永久解不開。
#   產生方式：openssl rand -base64 32
#   未設定時：人事模組不可用（拋 HrPiiKeyError → IS_CONFIG_MISSING），其餘功能不受影響。
#
# HR_PII_BLIND_INDEX_PEPPER
#   身分證字號盲索引（HMAC-SHA256）的 pepper，與資料金鑰分開管理。
#   **更換會使所有 national_id_hash 失效**並需重建帳本內唯一索引，因此視為一次性設定。
#   產生方式：openssl rand -base64 32
