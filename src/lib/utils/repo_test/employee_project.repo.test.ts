// ToDo: (20240830 - Murky) To Jacky - please help me edit this test
it('should be true', () => {
  expect(true).toBe(true);
});
// import { listEmployeeProject } from '@/lib/utils/repo/employee_project.repo';
// import employeeProjects from '@/seed_json/employee_project.json';

// describe('EmployeeProject Repository Tests', () => {
//   const testProjectId = 1000;

//   describe('listEmployeeProject', () => {
//     it('should return a list of employee projects for a given project ID', async () => {
//       const employeeProjectList = await listEmployeeProject(testProjectId);
//       expect(employeeProjectList).toBeDefined();
//       expect(Array.isArray(employeeProjectList)).toBe(true);
//       expect(employeeProjectList.length).toBeGreaterThan(0);
//       // Info: (20240704 - Jacky) Assuming the first entry in the seed data matches the first entry returned by the function
//       expect(employeeProjectList[0].id).toEqual(employeeProjects[0].id);
//       expect(employeeProjectList[0].employeeId).toEqual(employeeProjects[0].employeeId);
//       expect(employeeProjectList[0].projectId).toEqual(employeeProjects[0].projectId);
//     });
//   });
// });
