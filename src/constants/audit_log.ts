export enum AuditLogAction {
  CREATE = "CREATE",
  UPDATE = "UPDATE",
  DELETE = "DELETE",
}

export enum AuditLogDataType {
  JOURNAL = "JOURNAL",
  VOUCHER = "VOUCHER",
  ESG_RECORD = "ESG_RECORD",
}