// Info: (20260804 - Tzuhan) 逐章匯入頁碼切片的常數

/**
 * Info: (20260804 - Tzuhan) 切片上下界各留幾頁安全邊界。
 *
 * 邊界來自一次 LLM 頁碼索引呼叫,是**估計值不是事實**:同一份 PDF、同一組提示詞,
 * 兩輪可能給出不同答案。實測代價——第四章被標早一頁,第三章的切片因此斷在 p.41/42 之間,
 * 跨 p.42–44 的表3.8 只送進第一頁,半張表被驗證器丟掉;
 * 而表3.8 是桑基圖唯一的資料來源,於是整張圖消失,畫面上卻沒有任何異狀。
 *
 * 取 2 而不是 1:1 只擋得住剛好差一頁,而觀察到的誤差本來就不保證只有一頁;
 * 也不取更大值,因為重疊越多、同一節被多章送到的機會越高,逐項裁決的負擔就越重。
 * 這是拿 token 換「內容無聲消失」的風險,方向與本模組一貫的取捨相同。
 */
export const CARBON_PAGE_SLICE_MARGIN = 2;

// Info: (20260804 - Tzuhan) 頁碼下限(1-based):小於此值即索引壞掉,不是邊界情形
export const CARBON_PAGE_INDEX_MIN_PAGE = 1;

/**
 * Info: (20260805 - Tzuhan) 單次匯入呼叫最多涵蓋幾節。
 *
 * 目的是讓單次請求跑得完 —— 閘道的 `proxy_read_timeout` 預設 60 秒,
 * 而那是**閒置**逾時:請求在等 LLM 期間一個位元組都沒送,所以整段都算閒置。
 * 拉長逾時已被否決(全站共用、慢速連線攻擊成本降低),所以要縮的是工作本身。
 *
 * 取 4:實測 ch3(6 節)103 秒、ch1(7 節)41 秒,切成兩半即落在 60 秒內。
 * 不取更小是因為每次呼叫都要重送一次文件切片與提示詞,切太碎會讓
 * input token 與限流額度(12 次/分鐘)雙雙變成新的瓶頸。
 *
 * **已知救不了的情形**:節數少但單節極重。ch4 只有 3 節卻跑 2.5 分鐘,
 * 因為 4.2 一節裡有九張表(表4.1~4.9),其中表4.8 跨 8 頁 —— 輸出 20220 tokens。
 * 節是這個維度能切到的最小單位,再往下切就會把跨頁的表格切成兩半,
 * 那是拿「無聲的資料損失」換「請求跑得完」,方向錯了。
 * 那一節需要另外的處置(見 issue_drafts/2026-07-31/06)。
 */
export const CARBON_IMPORT_MAX_SECTIONS_PER_CALL = 4;
