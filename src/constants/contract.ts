export enum ContractStatus {
  VALID = 'Valid',
  EXPIRED = 'Expired',
  IN_WARRANTY = 'In Warranty',
  COMPLETED = 'Completed',
}

export enum ContractStatusWithAll {
  ALL = 'All',
  VALID = 'Valid',
  EXPIRED = 'Expired',
  IN_WARRANTY = 'In Warranty',
  COMPLETED = 'Completed',
}
