/**
 * Info: (20260905 - Luphia) 薪資紀錄完整度的掃描上限（#6774）。
 *
 * ## 這道上限擋的是離譜的日期，不是效能
 *
 * Info: (20260906 - Luphia) 初版寫 120（十年），理由是「迴圈成本」——
 * 而那個理由量過之後不成立：一百位員工、每人從 1990 年逐月展開到現在
 *（432 圈 × 100 = 43,200 圈），**耗時 0 ms**。
 *
 * 真正的代價在另一邊：**十年年資在台灣一點都不罕見**，而超過上限時這支
 * 回的是空陣列 —— 畫面完全不標示。一位 2015 年到職的資深同仁，就算一張
 * 薪資單都沒建過也不會被提示，而且沒有任何地方說得出為什麼。
 *
 * 600（五十年）涵蓋任何真實年資，同時仍然擋得住「到職日填成 1900 年」
 * 這種明顯錯誤的資料（產品決策 20260906）。
 *
 * 另外，當初一併擔心的「畫面被三百多個月份塞爆」不歸這裡管 ——
 * 提示框由 `previewMissingPeriods` 只取前 6 個、其餘由
 * `missing_records_rest` 說出還有幾個（`coverage_alert.tsx`）。
 * 顯示的截斷與判斷的上限是兩件事，混在一起的話這個數字會被當成版面參數。
 *
 * Info: (20260908 - Luphia) 這一段原本指的是 `formatMissingPeriods` 與
 * 文案「等 N 個月」（review 應修-2）—— 前者已刪、後者已換。
 * 照舊敘述去改的人會改到一個畫面上不存在的東西。
 *
 * 超過上限時**不下結論**（回空），而不是回一個截斷的清單 ——
 * 「缺這 600 個月」既沒用也不對。
 */
export const SALARY_COVERAGE_MAX_SCAN_MONTHS = 600;
