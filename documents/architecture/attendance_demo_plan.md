# 🎬 簽到系統 Demo 開發計劃書 (Time & Attendance Demo Plan)

> **Date**: 2026-08-13　
> **Author**: Julian　
> **Version**: 3.0（開發完成後精簡）
> **Status**: ✅ 開發完成（W1–W12 全數交付），待演示
> **母文件**: `documents/architecture/time_attendance_module_plan.md`（v1.3）
> **展示資料**: `attendance_demo_mock_data.md`　**執行手冊**: `attendance_demo_runbook.md`
> **展示對象**: 工程主管機關　**演示地點**: 新北市　**演示日期假設**: 2026-08-13（週四）14:00

> **v3.0 說明**：開發已完成，本文件從「要做什麼」改寫為**「做了什麼、為什麼這樣做、踩到什麼」**。
> 已刪除工作分解、估時、復原成本評估、既有資產盤點等只在開發前有用的內容；
> 保留設計決策的理由與 §6 的踩雷紀錄 —— 那兩者在維護與接手時才是有價值的部分。

---

## 🎯 1. Demo 要證明的五件事（兩段，約 9 分鐘）

兩段的目標聽眾不同：第一段講給**工務與職安**，第二段講給**主管與稽核**。

### 第一段：「工地上現在有誰」（約 4 分鐘）

| #      | 主張                                                             | 演示方式                                                          |
| ------ | ---------------------------------------------------------------- | ----------------------------------------------------------------- |
| **P1** | 員工用既有的公司 Google 帳號就能簽到                             | 點「使用 Google 登入」→ 打卡頁顯示自己的姓名、工號與今日班別      |
| **P2** | **人不在現場就打不了卡** —— 圍欄是到班的定義，不是事後標記       | 現場打卡成功；切遠處座標 → 403「距工區 3.2 公里」                 |
| **P3** | 系統隨時答得出「哪個工地現在有幾個人、分別是誰、誰排了班卻沒到」 | 現場頁：地圖 + 在班／未打下班卡／未到工三個數字 + 名單 + CSV 匯出 |

### 第二段：「出勤對不對得上」（約 5 分鐘）

| #      | 主張                                                           | 演示方式                                                                                         |
| ------ | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **P4** | **一套班別模型同時支援固定班與彈性工時，判定引擎沒有任何分支** | 兩位同仁同樣 09:47 打卡：固定班遲到 47 分、彈性班正常。然後投出程式碼，指出沒有 `if (shiftType)` |
| **P5** | 系統自動比對班表與打卡，標出遲到、早退、曠職、漏打卡           | 出勤總覽方格圖，異常標色；點開看分鐘數                                                           |

**P4 才是這套系統與眾不同的地方**，而它完全看不見 —— 除非把程式碼投出來。P5 是任何一套考勤系統都有的功能。

**不演示**：補打卡申請單、多帳本、權限矩陣、限流、瞬移偵測 (G5)、網段驗證 (G4b)。
其中補登單最需要一句話帶過 —— 圍欄外一律拒絕之後，它是**外勤到班事實的唯一入口**（母文件 §D9），但它是一條流程（申請 → 通知 → 簽核 → 重算），演一半反而讓人以為系統沒處理外勤。

---

## 🧭 2. 八條核心設計決策

### 2.1 固定班表是彈性班表的特例 —— 因此沒有 `shiftType` 欄位（P4）

`ShiftPattern` 六個欄位全部 NOT NULL：窗起、窗迄、核心起、核心迄、應工作分鐘、休息分鐘。
**固定班就是「窗＝核心」的彈性班。** 因此：

- 資料庫沒有 `shiftType`（有的話它唯一能做的事就是在有人改了時間卻忘了改它的那天開始說謊）
- 判定引擎沒有 `switch`，四條規則覆蓋兩種制度
- 前端顯示的 `kind`（固定／彈性）是**衍生值**，由六個欄位的值算出來
- 未來加第三種制度，**改的是資料不是程式碼**

### 2.2 `User` 與 `Employee` 之間本來沒有橋

登入身分是 `User`，出勤事實掛在 `Employee`，母文件沒有處理這個缺口。
決策：`Employee.userId String? @unique` + 首登以**公司信箱**比對自動綁定。

**email 比對只是首次綁定的引導，綁定本身是那個欄位** —— 公司信箱可以變更（改名、調動），而打卡歷史不該跟著飄移。
且 `personalEmailCipher` 是密文、DB 端查不了，用個人 Gmail 登入的人本來就對不上（ADR 018 §7 的必然結果，不是 bug）。

同帳本內兩筆員工檔的信箱只差大小寫時**擋住不猜**（`CF_EMPLOYEE_EMAIL_AMBIGUOUS`）—— 任選一筆綁定就是讓某人以另一個人的身分打卡。

### 2.3 圍欄外一律拒絕，而「拒絕」不得外推

座標未落入任何圍欄的打卡**不會寫進資料庫**，直接 403。圍欄就是「到班」這個事實的定義本身 ——
人不在登記的地點，不是「到班了但有疑慮」，是到班沒有發生，而系統不記錄一件沒發生的事。

它也是 P3 能成立的前提：**若允許圍欄外打卡，「工地上有幾個人」這個數字立刻失去意義**，而那個數字在疏散時是要拿來對人頭的。

但這條線必須劃清楚，不能變成通則：

| 情況                   | 性質                     | 處置                                                                           |
| ---------------------- | ------------------------ | ------------------------------------------------------------------------------ |
| 圍欄命中與否           | 對「到班事實」的**定義** | 拒絕                                                                           |
| 定位精度不足           | **證據品質**不足以判定   | 拒絕，但訊息是「請重試」而不是「你不在現場」                                   |
| 瞬移偵測（G5，未實作） | 對紀錄可信度的**推測**   | 收下並標記 —— 有誤判率的啟發式一旦拒絕，就會出現「員工真的到了工地卻打不了卡」 |

### 2.4 砍掉的是快取，留下的是真相

Demo 不建 `AttendancePresence` / `AttendanceDailyResult` / `AttendanceException` 三張表，全部改為**讀取時即時推導**。

母文件 §D10.1 自己說過：「『誰現在在班』完全可由 `AttendancePunch` 推導……推導是正確的，但每次推導不可行。」
**不可行的理由是效能，而 demo 沒有那個壓力。** 一個月 × 12 人 = 372 次純函數呼叫。

這一刀省下的不只是三張表，還有同交易一致性、`rebuildPresence`、勾稽對帳、異常子表整批替換、以及整個背景 Worker。
**而 demo 寫的推導邏輯，正好就是正式版 `rebuildPresence` 與 Worker 內部要呼叫的東西。**

規模上去之後的正解是**快取**（可隨時丟棄、丟了自己會重建），不是落地（丟不掉，而丟不掉的東西就會被當成真相）。

### 2.5 判定引擎是純函數：不碰 DB、不呼叫 `Date.now()`

「現在」由呼叫端注入（`nowMinuteOfDay`）。同一組輸入永遠得到同一組輸出 ——
**可重算是這整套判定唯一的驗收方式。**

同理，A9 判定矩陣的 `evaluatedAt` 在 route 取一次往下注入，整張矩陣共用同一個時間點：
逐列各取一次「現在」，兩位員工卡在寬限那一分鐘的邊界案例可能在同一張表裡得到互相矛盾的顏色。

時區換算集中在 `attendance_time.ts`，引擎只做整數運算 ——
引擎的測試不需要任何時區設定，而時區的測試只需要針對那一個檔案。

### 2.6 `STALE` 的語意是「我不知道」，不是「他不在」

現場名單的資料來源只有打卡：打上班卡進名單、打下班卡出名單。問題出在**忘記打下班卡**。

| 做法           | 後果                                                                           |
| -------------- | ------------------------------------------------------------------------------ |
| 一直當他在現場 | 名單只進不出，一週後全是忘記打卡的人堆著。**這張名單就廢了**                   |
| 立刻當他不在   | 但他可能真的還在加班。**疏散點名漏掉一個還在樓裡的人，是這套系統最不能犯的錯** |

`STALE` 就是那條線：「等多久之後，系統承認自己不知道」。它不是懲罰也不是異常，是**誠實**。
因此 `STALE` 的人**不移出名單**，而且要標出來 —— 他們恰恰是點名時要優先打電話確認的對象。

看板上三個數字回答三個不同的問題，**缺一個就會暗示系統其實不知道的事**：

- **在班**：系統知道他在
- **未打下班卡**：系統**不知道**他在不在
- **未到工**：排了班、時間到了，系統沒有收到任何打卡

### 2.7 非法狀態擋在三層，各自服務不同的呼叫者

以「上班日必須有班別、非上班日必須沒有」這條不變式為例：

1. **TypeScript**：`IDaySchedule` 是可辨識聯集 —— 寫不出來（編譯期）
2. **Zod**：A8 的請求本體也是可辨識聯集 —— 送不進來（API 呼叫者）
3. **`assertSchedulableDay`**（repository）—— 擋種子腳本、資料遷移、排班表 Excel 匯入

第三層不是重複：前兩層擋走 API 的呼叫者，第三層擋繞過 API 的路徑，
而**排班表匯入正是一次寫入上千筆、最可能把兩個欄位配錯的地方**。

> **為什麼不拆表**：ADR 019 對 `ProcessTask` 的解法是拆表讓非法狀態不可表示。這裡拆成
> `ScheduledWorkDay` / `ScheduledOffDay` 會**弄丟 `@@unique([accountBookId, employeeId, workDate])`** ——
> 「同一人同一天既排班又排休」變成新的非法狀態。
> **判準不是「有沒有 discriminator 欄位」，是「拆完之後非法狀態的總量有沒有變少」。**

### 2.8 工時只輸出分鐘，不換算金額

加班費、時薪、假日加給屬薪資模組職責（ADR 020）。**分鐘 → 金額的乘法絕不在本模組發生。**
被問到加班費時的正確答案：「出勤系統負責把時間算對，錢由薪資模組算 —— 兩邊各算一次，遲早會對不起來。」

---

## ⚖️ 3. 判定表（12 條中實作 11 條）

由上而下，第一個命中即決定當日狀態。

| #   | 條件                                        | 狀態          | 異常                                   |
| --- | ------------------------------------------- | ------------- | -------------------------------------- |
| 1   | `dayType ≠ WORK` 且無打卡                   | `OFF_DAY`     | —                                      |
| 2   | `dayType ≠ WORK` 且有打卡                   | `OFF_DAY`     | — （**假日出勤是加班事實，不是異常**） |
| 3   | 無排班紀錄                                  | `NO_SCHEDULE` | — （**不判曠職**）                     |
| 4   | 應出勤、完全無打卡、已過窗迄                | `EXCEPTION`   | `ABSENT`                               |
| 5   | 應出勤、完全無打卡、當日未結束              | `NORMAL`      | —                                      |
| 6   | 有 `CLOCK_OUT` 無 `CLOCK_IN`                | `EXCEPTION`   | `MISSING_CLOCK_IN`                     |
| 7   | 有 `CLOCK_IN` 無 `CLOCK_OUT`、已過窗迄+寬限 | `EXCEPTION`   | `MISSING_CLOCK_OUT`                    |
| 8   | `firstIn > coreStart + 寬限`                | `EXCEPTION`   | `LATE`                                 |
| 9   | `lastOut < coreEnd − 寬限`                  | `EXCEPTION`   | `EARLY_LEAVE`                          |
| 10  | 工作分鐘 < 應工作分鐘                       | `EXCEPTION`   | `INSUFFICIENT_HOURS`                   |
| 11  | G5 瞬移偵測                                 | `EXCEPTION`   | `SUSPICIOUS_JUMP`（**未實作**）        |
| 12  | 以上皆不成立                                | `NORMAL`      | —                                      |

**8–10 可同時成立**，因此引擎回傳的是**異常清單**而不是單一狀態。
**工作分鐘** = `clamp(lastOut, 窗內) − clamp(firstIn, 窗內) − breakMinutes`，為負取 0。

### 三條「不判為異常」的刻意設計 —— 演示時比任何一個異常更值得講

| 情境                     | 為什麼不判                                                                                                   |
| ------------------------ | ------------------------------------------------------------------------------------------------------------ |
| **無排班**（#3）         | 判曠職等於系統自己發明了一個不存在的出勤義務。**沒有班表就沒有比較基準** —— 這是「零捏造」在本模組的具體形狀 |
| **當日未結束**（#5、#7） | 判定的前提是「這一天已經過完」。提早下結論會讓早班同仁每天早上都收到曠職通知                                 |
| **假日出勤**（#2）       | 標紅會讓真正的異常被淹沒                                                                                     |

> **它們證明系統知道自己不知道什麼。一套會把「沒排班」判成曠職的系統，產出的每一個數字都不能信。**

### P4 的四條規則對照（這張表就是演示腳本）

| 判定     | 統一規則                         | 本部行政班（固定）     | 工程師彈性班（核心 10–16）               |
| -------- | -------------------------------- | ---------------------- | ---------------------------------------- |
| 遲到     | `firstIn > coreStart + grace`    | 09:47 → **遲到 47 分** | 09:47 → **正常**                         |
| 早退     | `lastOut < coreEnd − grace`      | 17:30 → 早退           | 17:30 → 正常                             |
| 工時不足 | `工作分鐘 < requiredWorkMinutes` | 正常出勤時恆滿足       | 10:00–16:00 只有 360 < 480 → 不足 120 分 |
| 窗外時間 | 打卡先 clamp 進 `[窗起, 窗迄]`   | 08:00 到班不多算工時   | 06:00 到班不多算工時                     |

**同樣的 09:47，兩種制度得到相反結論，而中間沒有一行程式碼在區分它們。**

---

## 🔌 4. 交付內容

### 4.1 端點（10 支）

前綴 `/api/v1/user/account_book/[account_book_id]/hr/attendance/`，
一律 `getIdentityFromDeWT` → `safeParse` → service → `jsonOk` / `jsonFail`。

| #   | Method | 路徑                               | 說明                                                         |
| --- | ------ | ---------------------------------- | ------------------------------------------------------------ |
| A1  | `POST` | `/punch`                           | 打卡。body **無時間欄位**（G1）。圍欄外 403 帶最近地點與距離 |
| A2  | `GET`  | `/today`                           | 我今天的狀態：班別、是否在班、上下班時刻、所在地點           |
| A3  | `GET`  | `/presence`                        | 各地點在班／未打下班卡人數 + 全帳本未到工名單                |
| A4  | `GET`  | `/presence/location/[location_id]` | 該地點到班名單                                               |
| A5  | `GET`  | `/location`                        | 地點與圍欄清單                                               |
| A6  | `GET`  | `/shift_pattern`                   | 班別清單（含衍生的 `kind`）                                  |
| A7  | `GET`  | `/schedule?from&to[&departmentId]` | 排班月曆                                                     |
| A8  | `PUT`  | `/schedule`                        | 改單日排班（可辨識聯集）                                     |
| A9  | `GET`  | `/result?from&to[&employeeId]`     | 出勤判定矩陣。即時計算，區間上限 92 天                       |
| A10 | `POST` | `/presence/roster/export`          | 緊急點名 CSV，含產出時間戳與產出者，寫 `AuditLog`            |

地點與班別的建立走 seed，不做 CRUD 端點。

**新增錯誤碼**：`FO_PUNCH_OUT_OF_FENCE`(FO000009)、`VA_PUNCH_LOW_ACCURACY`(VA000042)、
`VA_PUNCH_INVALID_STATE`(VA000043)、`VA_ATTENDANCE_RANGE_TOO_LARGE`(VA000044)、
`VA_SCHEDULE_DAY_INVALID`(VA000045)、`NF_EMPLOYEE_FOR_USER`(NF000017)、
`NF_WORK_LOCATION`(NF000018)、`NF_WORK_LOCATION_UNKNOWN`(NF000019)、
`NF_EMPLOYEE`(NF000020)、`NF_SHIFT_PATTERN`(NF000021)、
`CF_EMPLOYEE_ALREADY_LINKED`(CF000004)、`CF_EMPLOYEE_EMAIL_AMBIGUOUS`(CF000005)。

### 4.2 前端（4 頁）

| 路徑                                 | 內容           | 關鍵設計                                                                                                                             |
| ------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `/hr_management/attendance`          | 打卡頁         | 定位分五種狀態、四種畫面（等一下／可以打卡／去設定改權限／換一台裝置）；**圍欄外時按鈕仍可按** —— disable 的按鈕不會告訴任何人為什麼 |
| `/hr_management/attendance/schedule` | 排班月曆       | 方格圖 + 星期標頭；點格開編輯面板；班別簡稱**唯一性是算出來的**                                                                      |
| `/hr_management/attendance/presence` | 現場人數與名單 | 三數字看板 + 真尺寸圍欄地圖 + CSV 匯出；15 秒輪詢；**地圖上沒有任何個人位置**                                                        |
| `/hr_management/attendance/result`   | 出勤總覽       | 方格圖 + 圖示 + 多重異常標記 + 明細面板                                                                                              |

### 4.3 Demo 參數

| 參數                                                         | 值  | 說明                                                                       |
| ------------------------------------------------------------ | --- | -------------------------------------------------------------------------- |
| `DEMO_GEOFENCE_RADIUS_METERS`                                | 500 | 對工區接近可用值；**對辦公室過寬**，故展示資料把本部設 300                 |
| `DEMO_MAX_ACCURACY_METERS`                                   | 200 | 護欄 G3。超過即拒收，訊息是「請重試」不是「你不在現場」                    |
| `DEMO_LATE_GRACE_MINUTES` / `DEMO_EARLY_LEAVE_GRACE_MINUTES` | 5   |                                                                            |
| `DEMO_MISSING_CLOCK_OUT_GRACE_MINUTES`                       | 3   | 服務**工時計算**，寧可晚一點判                                             |
| `DEMO_PRESENCE_STALE_MINUTES`                                | 3   | 服務**安全**，寧可早一點承認不知道。**與上一條刻意分成兩個常數，即使同值** |
| `DEMO_ATTENDANCE_MAX_RANGE_DAYS`                             | 92  | 「即時計算不落地」只在成本有界時成立                                       |

---

## 🪤 5. 開發期間發現、並回頭修正計畫書的九處

> **這一節是本文件最該保留的部分。** 每一條都是實作時才浮現、光看規格看不出來的問題。

| #   | 發現於 | 原本寫的                                 | 實際上                                                                                                                                                                                                                                                                                                                                      |
| --- | ------ | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | W8     | 手動座標開關「與護欄 G2 直接衝突」       | **那句話是錯的。** 伺服器本來就不信任 client 送來的任何座標，用 curl 送假座標一直都做得到。這個輸入框只是把那件事做成一般使用者按得到的按鈕。因此旗標只需要 `NEXT_PUBLIC_` 這一支（純前端），伺服器沒有、也不該有對應開關 —— 加一個等於承認伺服器原本信任 client 座標。**移除它的理由是「不要留一個現成的作弊入口」，不是「它繞過了護欄」** |
| 2   | W6     | 判定結果只有 `status`                    | 引擎的 `NORMAL` 意思是「目前查不到異常」，對**還沒開始**的工作日回的也是 `NORMAL`。前端照 status 上色，下個月每一格都會是綠的 —— 那是對尚未發生的日子宣稱「這天正常出勤」。因此每格另帶 `phase`（未開始／進行中／已結束），而邊界是**該班別的窗迄加寬限**，不是日曆換日（夜班的 8/12 要到 8/13 清晨才結束）                                 |
| 3   | W6     | `VA000044` 保留給 `VA_PUNCH_NO_SCHEDULE` | Demo 已決定無排班允許打卡，那個碼不會被建立。為一個可能永不存在的碼留號段，只會在流水號裡留一個沒人解釋得清楚的洞                                                                                                                                                                                                                           |
| 4   | W11    | 出勤總覽只用顏色分辨六種狀態             | 這裡的主角正是**紅與綠**，而紅綠是最常見的一型色覺辨識障礙 —— 純靠顏色，這張表對那些人等於空白，而它在工地上是要拿來對出工的。每格加圖示。另外一格只有一個顏色、一天卻可以有多種異常，因此需要嚴重度順序（曠職 > 漏打卡 > 遲到／早退 > 工時不足）+ 右上角小點示意「還有」—— **看不到的資訊等於不存在**                                      |
| 5   | W7     | 「未到工」門檻是**窗起**                 | 彈性班窗起 07:00、核心起 10:00 —— 以窗起為門檻，每天七點過後所有彈性班同仁都被列成未到工，而那是他們正當的彈性。**一個每天早上都在報警的數字，不會有人看第二次。** 改取「核心起 + 遲到寬限」，即系統開始判遲到的那一刻                                                                                                                      |
| 6   | W7     | 「未到工」按地點顯示                     | 這些人沒有打卡 → 系統手上沒有座標，而 `EmployeeShiftDay` 也沒有地點欄位，**硬分配就是捏造**。改為只給全帳本總數與名單。正解是排班帶預定工區（母文件 §D10.6 已補 ToDo）                                                                                                                                                                      |
| 7   | W9     | 地圖標記數字＝**在班人數**               | `STALE` 的語意是「不知道他在不在」。把他從地圖的數字裡拿掉，就是**把「不確定」顯示成「不在」，而那是母文件 §D10.4 自己點名的「這類系統最危險的失真」**。疏散時要問的是「這個工區裡最多可能有幾個人」。改為在班 + 未打下班卡                                                                                                                 |
| 8   | W5     | 未提跨帳本檢查                           | `EmployeeShiftDay` 的 `employeeId` 與 `shiftPatternId` 在資料庫層**沒有任何跨帳本約束** —— 光靠 id 查得到就寫下去，等於租戶隔離破了一個洞，而症狀要等到有人發現自己的班表出現沒看過的班次才會浮現                                                                                                                                           |
| 9   | W10    | 格子上用一個字的班別簡稱                 | 資料裡**沒有這一欄**。取名稱首字看似可行，直到帳本同時有「工地日班」與「工程師彈性班」—— 兩個都是「工」。碰撞的症狀是「排班表看起來完全正常，只是有兩欄的意思是反的」。改為取**能讓全體互不相同的最短前綴**（一字 → 兩字 → 退回 `code`）                                                                                                    |

### 同時回頭修正母文件三處（v1.2 → v1.3）

| #   | 節     | 修正                                                                                                                                                                                                                                                                                                    |
| --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | §D5    | 地圖標記數字改為「在班 + 未打下班卡」（同上第 7 條）                                                                                                                                                                                                                                                    |
| 2   | §D10.5 | `AuditLog.dataId` 原本要求填**操作者**的 `Employee.id`，與 `AuditLogDataType.EMPLOYEE_PII` 自己的契約矛盾 —— 那個 enum 要求填**被讀到的**員工，理由是「調查軸線是哪些**人**受影響」。照原文寫，「這名員工的資料被誰看過」這個最常問的問題完全答不出來。改為一人一筆；操作者本來就記在 `AuditLog.userId` |
| 3   | §D10.6 | 新增一項已知失真：「應到未打卡」分不到地點，並加 ToDo 要求排班帶預定工區                                                                                                                                                                                                                                |

### 另外三個實作時的技術陷阱

1. **Prisma 產生的是字面量聯集，`@/constants` 的是 TS string enum（名義型別）。**
   enum 成員 → 字面量聯集可以，反向**編譯失敗**。因此凡是從 DB 讀出來要餵進引擎的欄位都要明寫轉換，
   值域一致由 `hr_enum_mirror.test.ts` 保證。
2. **`OFFICE-98` 原設定 09:00–18:00 的餘裕為零** —— 窗長 540 扣休息 60 = 480，與應工作分鐘完全相等，
   於是任何一分鐘的遲到都會同時觸發 `INSUFFICIENT_HOURS`。**寫判定引擎測試時才發現**：
   在「窗＝核心」的嚴格固定班下，遲到必然連帶造成工時不足，除非應工作分鐘 <（窗長 − 休息）。已改為 18:30。
3. **`AttendancePunch` 的 `id` 刻意沒有 `@default(uuid())`** —— 它是 PII 加密 AAD 的一部分，
   而加密發生在 insert 之前，等資料庫產生就來不及了（ADR 018 §3）。

---

## 🎥 6. 演示腳本（約 9 分鐘）

> 逐步操作與前置確認見執行手冊 §7。本節是**台詞重點**。

### 第一段：工地上現在有誰

| 步  | 動作                                | 台詞重點                                                                                     |
| --- | ----------------------------------- | -------------------------------------------------------------------------------------------- |
| 1   | Google 登入                         | 「員工用公司既有的帳號，不必記第二組密碼」                                                   |
| 2   | 打卡頁：今日班別 + 距離             | 「在你按下去之前，系統就先告訴你打不打得成、以及你今天幾點該到」                             |
| 3   | 按「上班打卡」                      | —                                                                                            |
| 4   | 切現場頁，指**三個數字**            | **「未到工那一個，是排了班但今天沒有打卡紀錄的人 —— 出工差異不必比對報表，系統自己算出來」** |
| 5   | 開示範模式，輸入 3 公里外座標 → 403 | **「人不在現場，不是『到班但有疑慮』，是到班沒有發生。系統不記錄一件沒發生的事」**           |
| 6   | 回現場頁，指出名單**沒變**          | 「這就是第 4 步那個數字為什麼可信 —— 圍欄外的打卡從來沒進過資料庫」                          |
| 7   | 助手打卡 → 等 15 秒 → 名單多一人    | 「工安事故時，這張名單是要拿來對人頭的」                                                     |
| 7b  | 匯出 CSV，指出產出時間與產出者      | **「事故調查時，『這份名單是幾點幾分產出的』與名單本身同等重要」**                           |
| 7c  | 指台北港工區的 🟡 許家豪            | **「系統沒說他在、也沒說他不在，說的是『我不知道』—— 這一格是最優先要打電話確認的」**        |
| —   | **關閉示範模式**                    | ← 最容易忘的一步                                                                             |

> **第 5、6 步要連著講。** P2 單獨演示只是一個錯誤訊息；接上第 6 步之後，它才變成 P3 的地基。

### 第二段：出勤對不對得上

| 步  | 動作                                                | 台詞重點                                                                                                                                                     |
| --- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 8   | 排班月曆：許家豪的夜班列、8/7 停工欄                | 「夜間封路施工、因雨停工，排班表上都是一格。點一格就能調」                                                                                                   |
| 9   | 出勤總覽 8/12：林淑芬 🔴 47 分 vs 周欣怡 ✅         | **「同一天、同一分鐘、相反結論。因為一個是本部行政班、一個是工程師彈性班」**                                                                                 |
| 10  | **投出 `attendance_rules.ts`**                      | **「資料庫裡沒有『班別類型』這個欄位。固定班就是彈性窗收縮到跟核心時間一樣的彈性班 —— 所以判定引擎一條分支都不需要。要加第三種制度，改的是資料不是程式碼」** |
| 11  | 點開周欣怡 8/6：不遲到不早退，工時不足 90 分        | 「彈性工時真正要管的不是幾點到，是總時數夠不夠」                                                                                                             |
| 12  | 王雅琪 8/11 曠職                                    | **「這一天工務所回報 4 人出工，系統只有 3 筆打卡。這就是出工查核」**                                                                                         |
| 12b | 許家豪 8/12 漏打下班卡；**切回現場頁**，他此刻是 🟡 | **「同一件事的兩個面向：現在這張名單告訴職安『我不確定他還在不在』，隔天這張報表告訴管理單位『這天要補登』」**                                               |
| 13  | 指今天那一欄的空白                                  | **「他今天還沒打卡，但系統沒有判他曠職 —— 現在才下午兩點。一套會在早上十點判人曠職的系統，產出的每個數字都不能信」**                                         |
| 14  | 8/7 停工欄、8/8 假日搶修的無異常格                  | 「假日到工是加班事實不是異常。把它標紅，真正的異常就會被淹沒」                                                                                               |
| 15  | 收尾                                                | 「打卡有異常時，員工線上填補登單、主管確認後系統自動重算 —— 這部分在正式版」                                                                                 |

> **第 10 步是整場對技術聽眾價值最高的一分鐘**，也是唯一需要投影程式碼的地方。
> **第 4、7c、12、12b 步是對工程機關價值最高的四分鐘。**
> **第 13 步不要跳過** —— 它決定了聽眾要不要相信前面所有的數字。

### 一個現成的敘事：EMP005／EMP006 從「未到工」變成「在班」

Seed 刻意不產生上台兩位的今日打卡。演示開始時他們**已經出現在未到工名單上**（核心起早已過），
上台打卡後轉為在班 —— 數字從「未到工 3」變成「未到工 1」。
**那是系統自己完成的敘事，比任何投影片都有說服力。**

---

## ⚠️ 7. 風險、清理與補完順序

### 7.1 演示風險（現場處置見執行手冊 §7，備援見 §8）

| 風險                                  | 機率   | 影響                | 緩解                                     |
| ------------------------------------- | ------ | ------------------- | ---------------------------------------- |
| 會場定位漂移超過圍欄                  | 低     | P1/P2 全毀          | T−5 實地校準；備案是示範模式輸入校準座標 |
| 定位權限被拒                          | 中     | 打不了卡            | 五種狀態各有明確指引；彩排               |
| 會場網路擋 Google                     | 中     | P1 全毀             | 手機熱點                                 |
| 上台者 Google 帳號 ≠ `Employee.email` | 中     | 卡在 404            | seed 前逐一確認，且 `userId` 須為 null   |
| **`HR_PII_KEY_V1` 未設定**            | 低     | 打卡全數 500        | **症狀最不直觀的一項**；seed 會直接中止  |
| seed 沒涵蓋到某種異常                 | **中** | P5 講到一半沒有例子 | **上台前自己把方格圖看一遍**             |

### 7.2 Demo 後必須清理（`Deprecated:` 標記，Release 前清查會抓到）

| #   | 項目                                                        | 為什麼必須移除而不是保留                                                     |
| --- | ----------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 1   | `NEXT_PUBLIC_DEMO_ALLOW_MANUAL_COORDINATE` 與手動座標輸入框 | 它把「客戶端指定自己的位置」做成一般使用者按得到的按鈕。改成「預設關閉」不夠 |
| 2   | `DEMO_GEOFENCE_RADIUS_METERS = 500`                         | 母文件 §D6 要求實地量測。500 公尺對辦公室意味著「在對面咖啡廳也算到班」      |
| 3   | `DEMO_ACCOUNT_BOOK_ID` 與前端寫死的帳本 id                  | 正式版帳本由使用者在選單裡挑                                                 |
| 4   | `ROSTER_CSV_LABELS_ZH_TW`                                   | CSV 欄位標題寫死一個語系；正式版應依請求者語系取字典                         |

### 7.3 Demo 之後的補完順序（依風險排序，不是依功能大小）

| 順位  | 項目                                                       | 為什麼是這個順序                                                                                                                                                                                                                                                                                                     |
| ----- | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **權限矩陣 + 隱私政策**                                    | Demo 沒有權限控制，登入者看得到全帳本名單、也改得了任何人的班。**這份程式碼一旦接上真實員工資料就是全員位置揭露事故**。因此 demo 環境**不得匯入真實個資**                                                                                                                                                            |
| **2** | **`WorkDayType.SUSPENDED`（停工）+ `suspensionReason`**    | Demo 暫借 `HOLIDAY`。混用會讓「今年停工幾天」與「今年國定假日幾天」變成同一個數字 —— **前者是工期展延與契約計價的依據**                                                                                                                                                                                              |
| **3** | **排班異動軌跡**（母文件沒有這一節）                       | 判定是即時算的，**改排班就是改歷史判定**：把 8/12 從上班日改成休假，那天的曠職當場消失，而沒有任何地方記得誰改的、原本是什麼。打卡本身 append-only，但它的**比較基準**可以被無聲地改掉。兩條路：排班改 append-only 加生效版本（貴，能重建任一時點），或新增排班類別的 `AuditLogDataType`（便宜，足以回答「誰改的」） |
| 4     | `ShiftAssignmentRule` 週期性指派                           | 沒有它，HR 要逐日建檔：22 天 × 50 人 = 1,100 筆                                                                                                                                                                                                                                                                      |
| 5     | 補打卡申請單                                               | 圍欄外一律拒絕之後，它是**外勤到班事實的唯一入口**                                                                                                                                                                                                                                                                   |
| **6** | **線形工程的帶狀圍欄（PostGIS）**                          | 道路與管線用圓形圍欄本來就不合用。**這是工程機關第一個會問的技術問題**                                                                                                                                                                                                                                               |
| 7     | `AttendanceDailyResult` / `AttendancePresence` 落地 + 勾稽 | **在資料量還小的時候補，是最便宜的時機**                                                                                                                                                                                                                                                                             |
| 8     | 限流、`EMERGENCY_ROSTER` 權限                              | 點名匯出的 `AuditLog` 已實作，缺的是權限與限流                                                                                                                                                                                                                                                                       |

---

> **相關文件**：`attendance_demo_mock_data.md`｜`attendance_demo_runbook.md`｜`time_attendance_module_plan.md`（母文件 v1.3）｜`decisions/018_hr_pii_data_classification.md`｜`decisions/019_*`
