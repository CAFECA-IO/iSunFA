# 可中斷／可接續的高耗點任務

- 版本：v1.0（2026-08-26，Luphia）
- 來源：PR #6717（issue #6712 / #6713 / #6714）與其兩輪 review、六輪 self-review
- 範圍：所有「一次操作會 fan-out 成多次計費呼叫」的功能。目前只有智能溫盤的逐章匯入，
  但這份文件描述的是**契約**，不是那一個功能

---

## 1. 為什麼需要它：一個具體的事故

64 頁的溫盤報告分 11 章匯入（實際切成 **14 個工作單元**），做到第 5 章時點數用完。畫面說：

> 以下章節解析失敗，可稍後重新匯入補齊：第5章、第6章……

而那 7 章**一個字都沒讀過**——`spendCredits` 在呼叫 LLM **之前**就擋下了，一點都沒扣。使用者因此回去改檔案；按「重新匯入」則再撞一次同一面牆。

成因是兩個「額度」被當成同一件事：

| 錯誤碼 | 是什麼 | 正確處置 |
|---|---|---|
| `IS_LLM_QUOTA_EXCEEDED` | **LLM 供應商**的配額 | 稍後重試會好 |
| `TW_QUOTA_EXCEEDED` | **使用者的點數**用盡 | 重試永遠不會好；等重置／加購／升級 |
| `TW_PERSONAL_PAYMENT_REQUIRED` | 無帳本會話，要個人付款 | 付掉訂單後以同鍵重送 |

前端唯一的守衛 `isQuotaApiError` 檢查的是第一個，於是後兩者落進「一般失敗」，而匯入的一般失敗文案就是「章節解析失敗」。

---

## 2. 核心區分：暫停不是失敗

> **做不了 ≠ 做壞了。**

| | 失敗 | 暫停 |
|---|---|---|
| 那一步做了嗎 | 做了，壞了 | **一步都沒做** |
| 扣到錢了嗎 | 可能扣了（結算或退款處理） | **一點都沒扣** |
| 重試有意義嗎 | 有（可能成功） | 沒有——先補點數 |
| 該不該停掉整趟 | 不該（其他步驟照跑） | **該**（剩下的必然撞同一面牆） |
| 畫面該說什麼 | 「這幾章解析失敗，可重試」 | 「點數用完，已完成 N／剩餘 M，這裡是三條出路」 |

兩者在資料上也必須是兩份清單。混成一份的代價實測過三次：使用者去改檔案、按重試再撞牆、以及**章節從畫面上整批消失**（暫停不進 `failed`，而整批取代 `failed` 就把它們清空了）。

---

## 3. 架構：書籤在伺服器，內容在客戶端

個人會話的匯入內容是**端到端加密**的（`CarbonPendingImport`，伺服器沒有金鑰）。因此：

```
┌─ 客戶端 ───────────────────┐        ┌─ 伺服器 ──────────────────┐
│ CarbonPendingImport（E2EE）│        │ ResumableJob（書籤）      │
│  · 解析結果（內容）        │        │  · 步驟 id 與計數         │
│  · pausedUnits（接續用）   │        │  · pauseReason / status   │
│  · pausedChapters（顯示）  │        │  · teamId / nextStepCost  │
│  ↑ 使用者的接續讀這一份    │        │  ↑ 掃描行程讀這一份       │
└────────────────────────────┘        └───────────────────────────┘
```

**這張表是書籤，不是容器。** 把內容搬進伺服器等於為了「可續跑」破掉 E2EE，因此伺服器的職責只到「**什麼時候可以繼續**」，執行永遠由持有金鑰的那一端進行。

### 已知的代價

- **兩軌可能不一致**（split brain）：兩邊的寫入都是 best-effort。目前**無害**，因為畫面只讀客戶端那一份；但 `GET /user/job` 一接上畫面，就必須先決定**誰說話算數**——否則「書籤說暫停、blob 說完成」會變成使用者可見的矛盾。
- **換裝置**：blob 跟著帳號走，但**原始檔案只在記憶體**。接續時檔案不在就要說出來（請使用者重新上傳，並明說已完成的不會重跑）——靜靜返回會變成一顆沒反應的按鈕，那是最難自救的失敗。

---

## 4. 驅動器的契約（`src/lib/jobs/resumable_job.ts`）

純函式，不碰網路也不碰 React。

- 步驟失敗分三類（呼叫端提供 `classify`）：`PAUSE`（做不了，原地可續）、`RETRY`（暫時性，同趟可再試）、`FAIL`（真的壞了，記下來繼續跑其他步驟）。
- 遇到**第一個** `PAUSE` 就停掉整趟。併發下可能有兩步同時撞牆，只認第一個原因。
- **`remaining` 以「有沒有結果」判斷，不是「游標之後」。** 併發下暫停發生時，另一條 worker 可能正跑在更前面的索引上，那一步也沒有結果——用游標判斷會把它靜靜丟掉。
- 粒度是**步驟**，不是使用者看得到的分組。這一條是踩過才學到的：`settledChapterIds` 以「章」為鍵做正向標記時，「一份做完、另一份撞牆」的章會被整個排除——不在暫停名單、不在失敗名單，而合併出來的內容少了一半的節。**`failed` 以章去重是安全的（任一份壞掉就整章列入），同樣的手法用在正向標記上語意剛好翻過來。**

---

## 5. 「現在可以繼續了嗎」：判準必須與扣款端同一個

三條出路（等重置／加購點數／升級方案）最後都收斂成同一句話——**現在的餘額夠不夠**。因此只有一支掃描行程（每 5 分鐘），而不是三套偵測。

### 5.1 判準跟著各功能的扣點模式（`JOB_SPEND_MODE`）

這是這批最貴的一課。掃描行程原本一律用「額度足額」判斷，而匯入實際的扣點是**封頂放行**（`allowPartial: true`，按 token 計量、結算時追補）。落差不是保守，是**永不觸發**：

| 方案 | 視窗（取兩者較小） | 一份 2MB PDF 的預扣估算 | 足額判準 |
|---|---|---|---|
| 免費 | min(10, 40) = **10** | 677 | 永遠 false |
| 團隊 | min(100, 750) = **100** | 677 | 永遠 false |
| 企業 | min(1000, 7500) = **1000** | 677 | 視窗幾乎全新時才成立 |

也就是那支 worker、`RESUMABLE` 狀態、那張表全部是裝飾品——而**最容易撞牆的免費使用者永遠等不到**。

> **判準與扣款端不一致時，方向對不代表結果對。「寧可少說」必須量過數量級才算成立。**

新增任務型別時必須在 `JOB_SPEND_MODE` 宣告它的模式——那是一個**必須做的決定**，不是可以沿用預設值的參數。

### 5.2 刻意的保留

- **不查鏈上點數**：一輪 50 筆就是 50 次 RPC。少算它只會讓答案偏向「還不夠」，方向安全。
- **接續 API 不先檢查餘額**：真正的判斷在扣款那一層（有鎖、會真的動到錢）。先查會出現「檢查說夠、扣款說不夠」兩個答案，而使用者只相信後者。
- **`PAYMENT_REQUIRED` 的暫停不由額度翻面**：它要的是一筆簽章付款，只有使用者本人做得到；翻面會是假承諾。

---

## 6. 所有權：兩道防線，因為那是兩件事

書籤的唯一鍵是 `(resourceKey, type)`，**不含 userId**，而 `update` 會改寫 `userId`。而碳盤查的 `resourceKey` 是**可推導的**頻道（`carbon-chat-{鏈上公開位址}-{常數}`）。

| 防線 | 回答的問題 | 位置 |
|---|---|---|
| 資源所有權 | 這個**頻道**是不是你的 | Service，per `JOB_TYPE` 的 exhaustive switch |
| 任務歸屬 | 這**一列**是不是你的 | Repo，比對既有列的 `userId` |

兩道都要，因為下一個 `JOB_TYPE` 的 `resourceKey` **不一定是頻道**——第一道的規則到那時可能就不適用了。`switch` 的 `default` 是拒絕：新型別必須先回答「這個資源屬於誰」。

裁決放在**任何查詢之前**：先查帳本再裁決的話，被拒的請求仍然能探測「某個頻道有沒有綁帳本」。

---

## 7. 未綁帳本的會話：能不能中斷接續，取決於呼叫次數

無帳本走個人點數：每次呼叫先回 402 帶一張待付訂單，付掉後以**同一把冪等鍵**重送才放行。

| 路徑 | 呼叫次數 | 處理 |
|---|---|---|
| 聊天送出 | 1 | 付掉再重送 |
| 段落草稿 | 1 | 付掉再重送 |
| **單發匯入**（小型文字檔） | 1 | 付掉再重送 |
| **逐章匯入** | **14** | **在送出前擋下**並說清楚 |

14 次呼叫＝14 筆訂單與 14 次簽章，不是任何人設計過的流程。若要支援，需要的是「一次付清整份」的訂單設計，而不是 14 筆。

> 冪等鍵在重送之間**必須相同**。這一條在加上重送之前不會出問題，是被重送帶出來的——加了重送而不改冪等鍵就是「付了一張、又建一張」。

---

## 7b. 檢查點：暫停是正常結束，其他每一種中斷都不是

成果原本**只在整段跑完之後才落地**。「點數用完」那條路一直是對的，因為它是正常結束——迴圈自己跳出來，後面的落地照跑。壞的是其他每一種中斷方式，而測試全都只走前者。

| 中斷方式 | 修正前 | 修正後 |
|---|---|---|
| 點數用完（暫停） | 進度有存 ✅ | 不變 |
| 關掉分頁 / 重新整理 | **全部白做** | 存到最後一份 |
| 單頁應用內導航 | **全部白做** | 存到最後一份 |
| 瀏覽器當掉、電腦睡著 | **全部白做** | 存到最後一份 |
| 非暫停的拋錯 | **全部白做** | 存到最後一份 |

14 份要跑 7～14 分鐘，單次預扣估算約 677 點——商業方案掉 6 份約 4,000 點。

兩個實作上的判斷：

- **剩餘以「有沒有結果」算**，與驅動器同一個判準。正在跑的那一份沒有結果，因此會被算進剩餘：那是安全的方向（下一次檢查點就會補上），而反過來會讓一份真的沒做的內容永久消失。
- **檢查點的 `pauseReason` 是 null**，所以書籤狀態是 `RUNNING` 而不是 `PAUSED`。掃描行程只看 `PAUSED`，因此不會去碰它——沒有人在等額度，把它翻成「可以繼續」會是一個假承諾。

「中斷」與「暫停」在畫面上是**兩句話**。共用一句的話，中斷時會說「點數已用完」，而使用者會跑去買他根本不需要的點數。

---

## 7c. 執行許可：租約，不是旗標

要防的事很具體：同一個帳號開兩個分頁（很常見——第一個看起來卡住了才開第二個），補點數之後兩邊都跳出「可以繼續」，兩邊都按下去 → 同一批份送兩次 → **點數扣兩次**。`isRetryingImport` 只擋得住同一個分頁。

**租約不需要新欄位**：`status === RUNNING` 加上 `updatedAt` 的新鮮度就是租約，而 §7b 的檢查點每做完一份就寫一次書籤——天然就是續租的心跳。租期 5 分鐘，取法是「必須大於兩次心跳之間的最長間隔」，也就是單一份的最長耗時。

三個非顯而易見的決定：

1. **裁決寫在 `updateMany` 的 `where` 裡**，不是先讀再判斷。先讀後寫之間有窗口，而這把鎖的全部意義就是關掉那個窗口——「先 `findUnique` 判斷再 `update`」在單執行緒的測試裡與正確版本表現一模一樣。前面那次 `findUnique` 只用來**區分失敗的原因**，不參與裁決。
2. **許可按資源而不是按任務 id**。客戶端手上只有頻道（那是它自己的聊天室），任務 id 在伺服器。要它先查一次 id 再來換許可，等於在最要緊的路徑上多一個往返，而那個往返本身又是一個競態窗口。
3. **鎖自己壞掉時放行**。這把鎖是為了省錢，不是為了在它自己掛掉的時候把功能一起關掉。取捨的兩邊是「鎖掛掉時可能重複扣一次」與「鎖掛掉時誰都不能匯入」，後者明顯更糟。

租期一定要會過期：分頁被強制關掉、瀏覽器當掉、電腦睡著時，沒有任何人會來釋放——而永久鎖住的症狀是「按了沒反應」，最難自救的一種失敗。

| 意圖 | 沒有任務 | 已完成 | 別人正在跑 |
|---|---|---|---|
| `RESUME` | 404 | 400（收起按鈕） | **409（留著按鈕，請他等）** |
| `START` | 放行 | 放行（覆寫舊書籤） | **409** |

`START` 也要擋：兩個分頁各自從第一份開始匯入同一個聊天室的話，兩份帳都要付。

---

## 7d. 付款完成後自動接續：付款一定發生在另一個分頁

暫停時畫面上的兩條出路（加購點數、升級方案）都是 `target="_blank"` 開新分頁——那不是省事，是刻意的：費思是浮動於 `/user` 各頁的常駐視窗，原地跳頁會把尚未送出的訊息與整段對話一起清掉。

於是「付款完成的那一頁」**永遠不是**那個在等點數的頁面。它一無所知，而使用者得自己走回去按一次按鈕——他剛剛就是為了那件事付的錢。

機制是一則 BroadcastChannel 廣播（沿用 `THEME_SYNC_CHANNEL_NAME` 的前例）：

| 付款路徑 | 發佈點 |
|---|---|
| 簽章付款（`useOrderTransaction`） | `payment_success` 之後 |
| 刷卡結帳（`payment_modal`） | 伺服器確認履行之後 |
| 團隊額度扣抵（`useTeamQuotaPayment`） | `status = success` 之後 |

四個決定：

1. **只廣播「付款成功了」這一件事實**，不廣播金額、也不廣播「去接續」這個指令。頻道是同源共享的，收到的內容是**輸入**而不是指令——認不出的一律忽略，因為那一端接著要做的事是花錢。
2. **廣播不是授權**。收到的那一頁會再向伺服器換一次執行許可（§7c）。廣播只縮短了「知道」的延遲，沒有繞過任何裁決。
3. **在伺服器確認之後才發**。發在送出的那一刻等於廣播一個可能不成立的事實。
4. **不在接續前多問一次「餘額夠不夠」**。真正的判斷在執行時的扣款；多一次檢查就會有「檢查說夠、扣款說不夠」兩個答案，而使用者只相信後者。萬一還是不夠，匯入會再次暫停並說對原因，而那一次撞牆在呼叫 LLM 之前就被擋下，一點都不會扣。

三道閘門：暫停原因必須是「點數用完」（需要簽章的那種要本人簽名，不能代勞）、這一頁沒有在跑、以及伺服器同意。開跑之前先說一句話——畫面自己動起來而沒有任何說明，比不動更難理解。

沒有 BroadcastChannel（SSR、舊瀏覽器、隱私設定關掉）時安靜降級：這是一條**便利路徑**，掃描行程（≤5 分鐘）與手動按鈕都還在。

---

## 8. 檔案清單

| 層 | 檔案 |
|---|---|
| 常數 | `src/constants/resumable_job.ts`（狀態、暫停原因、型別、`JOB_SPEND_MODE`、`JOB_CLAIM_TTL_MS`、`JOB_CLAIM_INTENT`） |
| 純函式 | `src/lib/jobs/resumable_job.ts`（驅動器）、`src/lib/quota/spend_split.ts`（`canAffordSpend` / `usesSharedTeamQuota`，與扣款端共用） |
| Repository | `src/repositories/resumable_job.repo.ts` |
| Service | `src/services/resumable_job.service.ts` |
| API | `PUT /user/job/bookmark`、`GET /user/job`、`POST /user/job/[job_id]/resume`、`POST /user/job/claim` |
| 跨分頁 | `src/constants/credit_events.ts`、`src/lib/credit_events.ts`（付款完成的廣播） |
| Worker | `ResumableJobScan`（`scripts/run_worker.ts`，每 5 分鐘） |
| 前端（碳盤查） | `use_carbon_chat.ts`（驅動器接線）、`use_carbon_chat.helpers.ts`（`resolveCreditPauseReason` / `summarisePausedUnits` / `foldImportChunks` / `isJobBusyError`）、`import_preview.tsx`（暫停區塊與「接著匯入」） |

---

## 9. 尚未完成（有意識的延後）

| 項目 | 為什麼還沒做 |
|---|---|
| `GET /user/job` 與 `POST .../resume` 前端接線 | 接上之前要先決定客戶端與伺服器**誰是 source of truth**（見 §3 的代價）。它真正的價值是「換裝置後知道你有一份未完成的匯入」——那是 blob 做不到的 |
| `cancelJob` 沒有端點 | 與上一項同一個 PR |
| 重試與接續兩條路徑還沒有檢查點 | 那兩條的損失**有界**（原本的進度還在暫存裡，掉的只有這一趟剛做的幾份）；主路徑掉的是全部，所以先修主路徑。issue #6723 |
| 正在跑的那一份仍會重跑 | 伺服器已經扣了點、也可能已經算完，但客戶端沒拿到結果就等於沒有。要根治得讓伺服器存得下結果，而那與「書籤與內容誰說得準」是同一個問題。issue #6721 |
| 掃描的 `unknown` 只進 log | 卡住的任務數不會浮上來 |
